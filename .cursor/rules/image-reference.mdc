---
description: 图片引用前必须先搜索确认文件存在，使用 **/*.png 等模式搜索
globs: **/*.vue
alwaysApply: false
---

# 图片引用规范

当需要引用图片资源时，必须先使用 Glob 工具搜索整个项目确认图片存在，再修改代码。

## 搜索模式

- 搜索图片：`**/*.png`、`**/*.jpg`、`**/*.svg`
- 搜索特定名称：`**/top.png`、`**/Canceltop.png`

## 正确流程

1. 用户提供图片文件名
2. 使用 Glob 搜索确认文件存在
3. 确认路径后修改代码引用

## 错误示例

```vue
<!-- ❌ 未确认图片是否存在 -->
<img src="@/assets/icons/top.png" />
```

## 正确示例

```vue
<!-- ✅ 先搜索确认文件存在，然后引用 -->
<img src="@/assets/icons/newicon-2026/top.png" />
```
